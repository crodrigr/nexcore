server:
  port: 8080

spring:
  config:
    activate:
      on-profile: local

  application:
    name: nexcore-core

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
    

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: crodrigr.test1@gmail.com
    password: "xqwd vqvg pauk mptb"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    default-encoding: UTF-8

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:4200}
  mail:
    from: crodrigr@gmail.com
    from-name: NexCore Platform
    frontend-url: ${FRONTEND_URL:http://localhost:4200}

  




